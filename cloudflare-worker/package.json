{
  "name": "gemini-web-bridge-worker",
  "version": "4.3.4",
  "description": "Cloudflare Worker: Stateful Gemini Web-Bridge Edge Hub",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "test:watch": "node --test --watch 'tests/**/*.test.mjs'",
    "lint": "node --check src/index.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
